// favorite-helper: removed — logic consolidated into app.js to avoid duplicates
// File left intentionally minimal to avoid accidental double-loading of favorite logic.
// See js/app.js for the single source of truth for favorite handling (uses localStorage).